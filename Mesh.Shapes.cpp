
#include "Mesh.h"

void Mesh::sphere(double radius) {
	vertices.clear();
	Color color;
}

void Mesh::cube(double radius, uint sub) {
	
	for (size_t i = 0; i < sub; i++) {
		for (size_t j = 0; j < sub; j++) {

		}
	}

}

